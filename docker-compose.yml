base:
  image: ihsw/py-lyfe
web:
  extends:
    service: base
  ports:
    - 80:5000
test:
  extends:
    service: base
  command: py.test
test-dev:
  extends:
    service: test
  volumes:
    - ./app:/srv/app
